<UserControl x:Class="TME.CarConfigurator.Publisher.UI.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Width="70">Country:</TextBlock>
            <TextBox x:Name="Country" Width="20"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Width="70">Model:</TextBlock>
            <ComboBox x:Name="Models"  Width="150">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"></TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Width="70">Generation:</TextBlock>
            <ComboBox x:Name="Generations"  Width="150">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"></TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Width="70">Environment:</TextBlock>
            <ComboBox x:Name="Environments"  Width="150">
            </ComboBox>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" Margin="3">
                <Button x:Name="PublishPreview">Publish to preview</Button>
                <Button x:Name="PublishLive" Margin="10 0 0 0">Publish to live</Button>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
